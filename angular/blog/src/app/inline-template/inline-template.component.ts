import { Component } from '@angular/core';

/**
 * This component was generated using the command: "ng g c inline-template --inline-template"
 * Using this command, we can create a component for which, there is no component-name.component.html file,
 * instead, we write the html for the component in the @Component decorator itself.
 */
@Component({
  selector: 'app-inline-template',
  template: `
    <h2 class="custom">
      inline-template works!
    </h2>
  `,
  styleUrls: ['./inline-template.component.css']
})
export class InlineTemplateComponent {

}
